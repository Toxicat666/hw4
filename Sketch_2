function setup() { 
  createCanvas(400, 400);
} 

function draw() { 
  background(0);

 	for(var x=0; x<=height; x+=20){
		for(var y=0; y<=height;y+=20){
			fill(255,255,255);
		ellipse(x,y,20,20);
	}
	}
}
